# Employee-Management-System-Using-Python
Employee Management System is a Python-based console application that performs CRUD operations like adding, viewing, updating, and deleting employee records. It uses file handling for permanent data storage and applies OOP concepts, loops, conditionals, and exception handling through a menu-driven interface.
